# Extra Python deps installed into the Superset image at container start.
# The official Superset entrypoint (/app/docker/docker-bootstrap.sh) pip-installs
# anything found at /app/docker/requirements-local.txt before booting the app.
# Bind-mounting this file there is the supported way to add SQLAlchemy drivers
# without baking a custom Dockerfile.

# Driver for connecting Superset -> Apache Pinot.
pinotdb

# Driver for connecting Superset -> PrestoDB. SQLAlchemy URI is
#     presto://presto-coordinator:8080/hive/default
# (in-network port 8080, NOT host port 8086 — Superset is in the same
# docker network as the Presto coordinator).
pyhive[presto]>=0.7.0
